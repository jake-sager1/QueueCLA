import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#0a8ea0',
      },
      secondary: {
        main: '#373737',
      },
    },
  });

export default theme;